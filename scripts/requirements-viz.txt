# Requirements for scripts/build-brain-viz.py
# Install in a dedicated venv:
#   python3 -m venv .venv-viz && source .venv-viz/bin/activate
#   pip install -r scripts/requirements-viz.txt

psycopg[binary]>=3.1
pgvector>=0.2
numpy>=1.26
umap-learn>=0.5
hdbscan>=0.8.38
scikit-learn>=1.5
python-dotenv>=1.0
requests>=2.31
